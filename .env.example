# Copy this file to .env.local, .env.dev, or .env.staging and fill in values
APP_ENV=local
# Optional: force whether GET /healthz includes app_env (true/false). Omit for automatic: hidden in production only.
# INCLUDE_ENV_IN_HEALTHZ=true
DATABASE_URL=postgresql+asyncpg://user:password@host:port/database
DATABASE_URL_DIRECT=postgresql+psycopg://user:password@host:port/database
